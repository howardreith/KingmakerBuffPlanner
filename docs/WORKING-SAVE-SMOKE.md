# Working-Save Smoke

Status: DEFER — EVIDENCED

No project-owned `KBP_` save fixture exists on this desktop at intake. Every existing save is protected, including `KMG_` fixtures owned by another product. No save-backed command is authorized until the guarded harness can prove a unique working descriptor and a distinct immutable baseline.

The only permitted identities are:

```text
KBP_AUTOMATION_BASELINE   immutable; never selected for mutation
KBP_AUTOMATION_WORKING    disposable; only save accepted by runtime requests
```

Before activation, source-only filesystem integration tests must prove rejection of blank, missing, unknown, duplicate, baseline, non-`KBP_`, and traversal identities. The live harness must capture the complete native save catalog, match the working identity exactly and uniquely using safe descriptor fields, retain the exact native descriptor/receiver chain, invoke one proven normal game-thread load action, observe stable loaded-game fingerprints, detect any save-writing API activity, and remove all observation hooks.

The scenario itself performs no save, migration, rename, deletion, gameplay action, or input injection. A missing or ambiguous catalog, baseline correlation, unexpected native write, wrong campaign fingerprint, dialog, timeout, or hook-cleanup failure cannot PASS.

Canonical invocation will be documented only after the project-owned fixture and receiver contract are qualified. Until then, save-backed rows remain `DEFER — EVIDENCED`; NO-SAVE scenarios may proceed independently.
